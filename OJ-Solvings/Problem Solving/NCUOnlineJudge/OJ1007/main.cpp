#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n, tmpa, tmpb;
    cin >> n;
    vector<int> a(n), b(n);
    for (int i = 0; i < n; i++){
        cin >> tmpa >> tmpb;
        a[i] = tmpa;
        b[i] = tmpb;
    }
    int end = 0, count = 0;
    for()


}