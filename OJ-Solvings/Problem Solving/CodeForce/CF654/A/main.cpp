#include <iostream>
#include <vector>

using namespace std;

int main () {
    int t, tmp;
    cin >> t;
    vector<int> n(t);
    for (int i = 0; i < t; i++)
    {
        cin >> tmp;
        n[i] = tmp;
    }

}