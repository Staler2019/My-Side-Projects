#include "linked_list.h"
#include <iostream>

Element::Element(int element): num(element){}

void LinkedList::insert(int element) {
    if(this.quantity == 0) {

    }
    Element e(element, );
    this.ve.push_back()
}

void LinkedList::delete(int element) {

}